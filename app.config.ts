export default defineAppConfig({
  ui: {
    notifications: {
      position: "top-0 right-0",
    },
    notification: {
      default: {
        icon: 'i-heroicons-information-circle',
        timeout: 2000
      }
    }
  },
});
